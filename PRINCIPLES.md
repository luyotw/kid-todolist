# kid-todolist Principles

> 如果三個月後我回來看這份文件，發現自己違反了 **做了一堆跟核心無關的功能，「設任務、打勾、拿獎勵」反而沒做好**，我最不能接受。

## 北極星

一個公開的 web app，讓家長自訂小孩每天要做的任務，完成一項就打勾，全部完成就能得到家長設定的現實世界獎勵。家長在自己手機上操作，不需要給小孩手機。

任務可以按星期幾排程（上學日和假日不同），也可以臨時加。獎勵由家長自訂文字，app 不管兌現。

這個 app 不靠賺錢驅動，但如果有人願意付費也不排斥。核心價值是：取代口頭提醒，讓每天該做的事有一個看得見、勾得到的地方。

## 現況判斷

目前 kid-todolist 的狀態：

- v1 核心流程已做出來：React + Vite + TypeScript，任務 CRUD、按星期排程、臨時新增、打勾、全部完成顯示獎勵文字
- 資料還在 localStorage，沒有登入，只有自己這台手機能用
- 用過別人的 app（squirrel-acorns 松鼠橡實）覺得流程對，但它任務不能自訂、遊戲化很重，所以自己做一個輕量的

下一步（v2）要到的狀態：

- 家長用 Google 登入，資料存雲端、換手機也在
- 別人也能自己上手
- 獎勵從「純文字」進化成「簡單點數兌換」

但還不是一個能讓陌生人用的產品。

## 系統邊界

採 **獨立工具** 的設計立場。這個 app 就管一件事：每天的任務清單。

它的責任：
- 讓家長設定任務（按星期排程 + 臨時新增）
- 讓家長幫小孩打勾
- 全部完成時顯示獎勵

不是它的責任：
- 不管獎勵的兌現（那是家長自己的事）
- 不追蹤小孩的長期表現統計（以後再說）
- 不跟學校或其他系統聯動（以後再說）
- 不管家長之間分享任務模板（以後再說）

## 路線圖總覽

### v1：自己能用

目標：家長在手機瀏覽器上操作，能跑完「設任務 → 打勾 → 看到獎勵」的完整流程。

核心能力：
- 自訂任務項目
- 按星期幾排程任務
- 臨時新增任務
- 打勾標記完成
- 全部完成顯示獎勵（家長自訂文字）

完成標準：
- 自己每天用得起來，不需要回去用口頭提醒

現況：核心流程已完成（任務 CRUD、按星期排程、臨時新增、打勾、全部完成顯示獎勵文字），目前用 localStorage 儲存、沒有登入。

刻意不做：
- 帳號系統 / 登入
- 小孩自己操作的介面
- 點數經濟系統
- 長期表現追蹤

### v2：別人也能用

目標：讓陌生人也能用，資料存在雲端、跟著家長走。

技術決策：採 **Firebase**。登入用 Firebase Authentication（Google 登入），資料存 Firestore，每個家長的資料放在 `users/{uid}` 底下，用安全規則隔離。

核心能力：
- 帳號系統：Google 登入，每個家長有自己的資料
- 雲端同步與資料隔離：localStorage 的四份資料（任務 / 完成紀錄 / 臨時任務 / 獎勵）搬到 Firestore
- 手機上的體驗夠好：PWA，可加到主畫面、離線可用
- 簡單點數兌換：每個任務可設點數，完成就累積，家長設定的獎勵用點數兌換（只做簡單加減，不做會長出規則的複雜經濟系統）

完成標準：
- 不認識的家長能自己用 Google 登入、設定、使用，不需要你教
- 換一支手機登入同一個帳號，資料都還在

### v3 以後再說

可能的方向（不承諾，且都要先過紅線的複雜度評估）。下列多數參考自 squirrel-acorns（松鼠橡實）那套習慣養成 app，但它的遊戲化很重，跟本 app「設任務、打勾、拿獎勵」的核心定位不同，所以刻意往後放：

- 小孩自己登入勾選
- 放學提醒鬧鐘（時間到了提醒該完成哪一項）
- 長期表現追蹤（週統計、月曆熱度，類似 squirrel-acorns 的「我的小本本」）
- 進階遊戲化：放學過關遊戲、每日回顧貼紙、賓果、音效與撒花動畫（參考 squirrel-acorns，評估複雜度後再說）
- 任務模板分享

## 紅線

無論再怎麼有機會，這些事情不做：

- **不蒐集不必要的使用者個資或行為數據** — 用的人是小孩，隱私是底線
- **不犧牲使用體驗做商業化** — 不塞彈窗廣告、不在任務流程中間插廣告。不打擾的推薦可以接受，但不能干擾核心操作
- **不為了加功能把東西搞壞** — 功能要不要加，不是看它好不好，而是看做了會不會把東西搞壞。計算類的功能（如點數系統）要特別評估複雜度。簡單的「累積點數 → 兌換獎勵」加減操作可以做（v2）；一旦要長出加碼、機率、連續獎勵、賓果這類規則的複雜經濟系統，維持在 v3 再評估

## 不做清單

看起來像是該做、但刻意不做：

- **社交 / 社群功能** — 這是家長和小孩之間的工具，不是社群平台
- **原生 app（iOS / Android）** — 先做好 web app，手機開瀏覽器就能用
- **API / 開放給第三方串接** — 沒有這個需求，加了只是多一個要維護的東西
- **國際化 / 多語言** — 先做好中文版

## 關鍵跳躍

### 1. 從零到 v1（自己能用）

要補的不是功能多寡，而是：

- 一個能在手機瀏覽器上順手操作的介面
- 任務排程邏輯（星期幾 + 臨時新增）
- 資料儲存方案（v1 可以極簡，不需要後端也行）

### 2. 從 v1 到 v2（別人也能用）

要補的不是更多功能，而是：

- 帳號系統和資料隔離
- 陌生人能自己上手的 onboarding
- 穩定度和錯誤處理

## 驗證軸

- `v1`
  - 自己每天真的在用嗎？有沒有偷偷回去用口頭提醒？
- `v2`
  - 不認識的家長能不能不看說明就用起來？

## Adoption 風險

- **習慣遷移**
  - 口頭提醒已經是根深蒂固的習慣，app 要比口頭更方便才會被用。如果每天打開 app 設任務比直接講還麻煩，就不會用
- **小孩的配合度**
  - app 再好，如果小孩不買帳「打勾」這個儀式，整個流程就斷了。獎勵機制要對小孩有足夠吸引力

## 結論

這個 app 的核心不是做得多豐富，而是讓「設任務、打勾、拿獎勵」這件事在手機上比口頭提醒更順手。

如果這是對的，那 v1 的成敗關鍵不是功能多不多，而是每天打開它比直接講還方便。
